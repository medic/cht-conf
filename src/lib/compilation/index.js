const compileContactSummary = require('./compile-contact-summary');
const compileTasksAndTargets = require('./compile-tasks-and-targets');

module.exports = {
  compileContactSummary,
  compileTasksAndTargets,
};
